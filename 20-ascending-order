public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    List<Integer> list = new List<Integer>{a, b, c};
    List<Integer> sortedList = new List<Integer>(list);
    sortedList.sort();
    return list.equals(sortedList);
}